/** Default nearby SOS awareness radius (miles). Future admin settings can override. */
export const RIDER_SOS_NEARBY_RADIUS_MILES = 5;

/** Polling interval for active SOS feed refresh (ms). */
export const RIDER_SOS_FEED_REFRESH_MS = 30_000;
