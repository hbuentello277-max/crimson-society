/** Default nearby SOS awareness radius (miles). Keep in sync with rider_sos_nearby_radius_miles(). */
export const RIDER_SOS_NEARBY_RADIUS_MILES = 10;

/** Polling interval for active SOS feed refresh (ms). */
export const RIDER_SOS_FEED_REFRESH_MS = 30_000;
